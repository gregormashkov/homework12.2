public class Main {
    public static void main(String[] args) {
        leapYear(2022);
        getClientOS;
        distance;
    }

    // Задание 1.
    public static void leapYear(int year) {
        String name = "year";
        if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0) {
            System.out.println(year + "год является високосным");
            System.out.println(year + "год не является високосным");
        }
        // Задание 2.
        public static int getClientOS (String iOS) {
            String osName = "iOS";
            int clientOS = getClientOS(osName);
            if (name.equals("iOS")) {
                return 0;
            } else {
                return 1;
            }
        }
        // Задание 3.
        public static int distance (String deliveryDays) {
            String name = "deliveryDays"
            int deliveryDisntance = 95;
            int deliveryDays = 1;
            if (deliveryDisntance > 20) {
                deliveryDays++;
            }
            if (deliveryDisntance > 60) {
                deliveryDays++;
            }
            System.out.println("Потребуется дней: " + deliveryDays);


        }
    }
}
// сделано ДЗ